# Ignora a pasta node_modules (por ser gigante), então precisamos rodar 'npm install' para baixar as dependências (veja o package.json)
node_modules 

.DS_Store
dist
dist-ssr
*.local
*.lock
*-lock.json
yarn-error.log
*.pdf